"""Motet's MCP surface (motet#111).

Only the registry exists so far: the classification of every API operation as a tool, a
planned tool, or a written exclusion. The server itself waits on the invariant-12 picks
put to the owner on motet#111; nothing here imports the MCP SDK.
"""
