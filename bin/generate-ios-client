#!/usr/bin/env bash
# Regenerate the iOS client's Swift types from openapi.yaml. Run bin/generate-openapi first.
set -euo pipefail
cd "$(dirname "$0")/.."
exec uv run python ios/tools/generate_swift_client.py "$@"
